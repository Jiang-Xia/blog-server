---
description: blog-server 注释硬约束（utils 与 modules 新功能）
alwaysApply: true
---

# Commenting Hard Requirement

- 本项目新增代码必须遵循“关键逻辑有注释”的硬约束，默认使用中文简短注释。
- 注释要求是**必须满足**，不是“可选优化”；未满足时优先补注释再交付。
- 新增方法、关键变量、关键常量时，都必须有 1 行中文注释说明用途或规则。

## utils 约束

- 新增 `src/utils/**/*.{ts,js}` 文件时，导出函数必须有 1 行中文注释，说明用途或脱敏/转换规则。
- 若函数包含正则、递归、边界分支，需在对应代码块前补 1 行中文注释说明意图。

## modules 新功能约束

- 在 `src/modules/**` 新增 controller/service/dto/vo/entity 的“新功能代码”时，必须为业务入口或关键分支补中文注释。
- service 中涉及权限、状态流转、数据写入、副作用（消息/缓存/文件）时，相关代码块前必须有注释说明。
- controller 新增接口时，若存在特殊校验、兼容逻辑或返回结构约定，必须加注释说明“为什么这样做”。
- controller/service 新增 `private` 辅助方法时，必须写简短中文方法注释（推荐 JSDoc 一行）。
- 新增与安全相关的阈值常量（限流、锁定、过期、黑名单）时，常量定义处必须写中文注释，说明单位与含义。

## 变量注释细则

- “关键变量”包括：缓存 key、风控计数器、权限判断标志、复杂查询条件、跨函数复用的中间值。
- 临时循环变量（如 `i`、`item`）可不强制注释；但语义不直观的缩写变量（如 `sArr`、`rArr`）应改名或补注释。
- 禁止只写“赋值说明”类废话注释，注释必须说明意图、边界或业务规则。

## 示例

- `const LOGIN_LOCK_SEC = 15 * 60; // 登录封禁时长（秒）`
- `const refreshBlacklistKey = ... // refresh token 黑名单键，防止重放`
- `/** 登录成功后清理失败计数与封禁状态 */`

## 执行标准

- 注释保持简短，避免逐行翻译；重点解释“意图/规则/边界”，不写显而易见的废话。
- 如果用户明确要求“本次不加注释”，可按用户指令执行，但需在回复中说明这是例外处理。
